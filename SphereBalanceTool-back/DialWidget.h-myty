#ifndef DIALWIDGET_H
#define DIALWIDGET_H

#include <QWidget>
#include <QDateTime>
#include <QPainter>
class DialWidget : public QWidget
{
	Q_OBJECT

public:
	DialWidget(QWidget* parent = nullptr);
	~DialWidget();

	void setQianhouDegree(int qianhou_degree);
	void setZuoyouDegree(int zuoyou_degree);


protected:
	void paintEvent(QPaintEvent* event);

private:
	int m_qianhou_degree = 0;
	int m_zuoyou_degree = 0;
	double step = 0;
};
#endif